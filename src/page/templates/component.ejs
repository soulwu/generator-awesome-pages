import React, {Component, PropTypes} from 'react';

export default class <%= component %> extends Component {
  static displayName = '<%= module %><%= component %>';

  static propTypes = {
    // todo 添加需要的参数
  }

  render() {
    // todo 添加具体的内容
    return null;
  }
}
