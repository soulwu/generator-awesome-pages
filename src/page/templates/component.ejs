import React, {Component, PropTypes} from 'react';

import '../../assets/pages/<%= directory %>/<%= asset %>.scss';

export default class <%= component %> extends Component {
  static displayName = '<%= moduleName %><%= component %>';

  static propTypes = {
    // todo 添加需要的参数
    children: PropTypes.any
  }

  render() {
    // todo 添加具体的内容
    return null;
  }
}
