import React, {PropTypes} from 'react';

import '../../assets/pages/<%= directory %>/<%= asset %>.scss';

const <%= component %> = (props) => {
  // todo 添加具体的内容
  return null;
}

<%= component %>.displayName = '<%= moduleName %><%= component %>';
<%= component %>.propTypes = {
  // todo 添加需要的参数
  children: PropTypes.any
};

export default <%= component %>;
